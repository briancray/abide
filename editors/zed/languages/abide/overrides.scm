; The scopes `config.toml`'s `[overrides.*]` sections apply to. `string` is what opts an attribute
; value in to the tailwind server — a `class="…"` is where a `.abide` template's styling lives.

(comment) @comment

[
  (attribute_value)
  (quoted_attribute_value)
] @string
