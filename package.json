{
    "name": "abide",
    "version": "0.0.0",
    "type": "module",
    "private": true,
    "workspaces": [
        "packages/*"
    ],
    "scripts": {
        "test": "bun run build && bun test --parallel",
        "test:serial": "bun test",
        "test:changed": "bun test --parallel --changed",
        "e2e": "playwright test",
        "e2e:changed": "playwright test --only-changed",
        "e2e:failed": "playwright test --last-failed",
        "profile": "ABIDE_PROFILE=1 playwright test --project=dogfood profile.e2e.ts --reporter=list",
        "cli": "bun packages/abide/cli/index.ts",
        "abide": "bun packages/abide/compiler/check.ts packages/dogfood",
        "typecheck": "bun packages/abide/cli/index.ts check packages/abide packages/harness packages/dogfood",
        "shapes": "bun packages/abide/compiler/shapes.ts packages/dogfood --out .abide/shapes.json --pretty",
        "dev": "bun run --filter dogfood dev",
        "build": "bun run --filter dogfood build",
        "dogfood": "bun run build && bun run --filter dogfood start",
        "check": "biome check packages editors/tree-sitter-abide playwright.config.ts package.json tsconfig.json biome.jsonc",
        "check:fix": "biome check --write packages editors/tree-sitter-abide playwright.config.ts package.json tsconfig.json biome.jsonc",
        "lint": "biome lint packages editors/tree-sitter-abide playwright.config.ts package.json tsconfig.json biome.jsonc",
        "format": "biome format --write packages editors/tree-sitter-abide playwright.config.ts package.json tsconfig.json biome.jsonc"
    },
    "devDependencies": {
        "@biomejs/biome": "^2.5.9",
        "@playwright/test": "^1.62.1",
        "@types/bun": "latest",
        "typescript": "^7.0.2"
    }
}
