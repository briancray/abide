node_modules
dist
.abide
.DS_Store
bun.lockb

# playwright
test-results
playwright-report
blob-report

# The status page's JUnit reports. Under node_modules, which is already ignored, so
# this line is a note rather than a rule: node_modules/.cache/abide-status/
