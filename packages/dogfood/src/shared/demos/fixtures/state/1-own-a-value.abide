<script>
import { state } from 'abide'

// One cell, created once when this component sets up. `{count}` reads it and `count = n` writes it —
// there is nothing else to import and no hook to be inside.
const count = state(0)
</script>

<p>count {count}</p>
<button onclick={() => count++}>add one</button>
