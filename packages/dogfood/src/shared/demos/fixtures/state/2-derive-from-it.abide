<script>
import { state } from 'abide'

const count = state(0)
</script>

<!-- Using a state in an EXPRESSION reads it, so nothing here has to be declared as a dependency. -->
<p>count {count} · doubled {count * 2}</p>
<button onclick={() => count++}>add one</button>
