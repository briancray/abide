<script>
import { state } from 'abide'
import { turns } from '#shared/conversations'
import LastSpoken from '#ui/components/LastSpoken.abide'

const room = turns({ conversation: 'onboarding' })
const draft = state('How do I rotate an API key?')
</script>

<LastSpoken/>

<div class="row">
    <label>message
        <input bind:value={draft}>
    </label>
    <button onclick={() => room.publish({ speaker: 'you', text: draft })}>
        Send
    </button>
</div>
