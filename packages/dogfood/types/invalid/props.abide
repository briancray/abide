<script>
import { props } from 'abide'
import type { Book } from '../models.ts'

type Props = {
    book: Book
    pages: number
}

const { book, pages, missing } = props<Props>()
</script>

<article>
    <h3>{book.title}</h3>
    <p>{pages.toFixed(0)}</p>
    <small>{unbound}</small>
</article>
