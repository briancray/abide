<script module>
  import Demo from "$ui/components/Demo.abide"
  import Panel from "$ui/components/Panel.abide"
  import JsonResponseDemo from "$ui/demos/rpc/responses/JsonResponseDemo.abide"
  import ErrorResponseDemo from "$ui/demos/rpc/responses/ErrorResponseDemo.abide"
  import TypedErrorDemo from "$ui/demos/rpc/responses/TypedErrorDemo.abide"
  import RedirectResponseDemo from "$ui/demos/rpc/responses/RedirectResponseDemo.abide"
  import RawResponseDemo from "$ui/demos/rpc/responses/RawResponseDemo.abide"
</script>

<main class="page">
  <h1>Response helpers</h1>
  <p class="lead">A handler returns a plain value (auto-wrapped in <code>json</code>), makes that explicit with <code>json</code>, or ends the call another way entirely — <code>error</code>, <code>error.typed</code> and <code>redirect</code> all <strong>throw</strong>, and the router renders each at the status it asked for. Every one is consumed live in the browser below. Streaming responses (<code>jsonl</code>/<code>sse</code>) have their own page — see <a href="/rpc/streaming">Streaming</a>.</p>
  <Demo id="json" title="<code>json(data)</code>" server="src/server/rpc/rpcGreet.ts" client="src/ui/demos/rpc/responses/JsonResponseDemo.abide" tab="server">
    <JsonResponseDemo />
  </Demo>
  <aside class="aside">
    <p>The default needs no helper at all: return a plain value and it is serialized as JSON. <code>json(data, init?)</code> is the same thing made explicit, for when you also want to set a status or headers.</p>
  </aside>
  <Demo id="error" title="<code>error(status, message)</code>" server="src/server/rpc/rpcBoom.ts" client="src/ui/demos/rpc/responses/ErrorResponseDemo.abide" tab="server">
    <ErrorResponseDemo />
  </Demo>
  <aside class="aside">
    <p>A failure is a <strong>throw</strong>, not a return. <code>error(status, message)</code> does not hand a <code>Response</code> back — it throws an <code>HttpError</code>. That is what puts the failure in the memo slot's <em>error</em> channel and keeps it out of the value channel: an in-process caller reaches it with <code>catch</code>, <code>fn.error()</code> reports it, and a read at <code>ttl: ∞</code> never retains a 422 as that slot's value forever.</p>
    <p>Transport is the other half. The router renders the thrown error at its own status — before <code>onError</code>, because a declared 422 is not a bug in the app — and the browser proxy decodes the non-2xx back into the same <code>HttpError</code>, so the same <code>catch</code> reads the same <code>status</code> on both sides. Throwing also <em>retires</em> type machinery instead of adding it: a <code>throw</code> is <code>never</code> and a union absorbs it, so <code>({'{'} fail {'}'}) =&gt; fail ? error(503) : {'{'} greeting {'}'}</code> still infers <code>{'{'} greeting {'}'}</code> with no brand and no conditional.</p>
  </aside>
  <Demo id="typed" title="<code>error.typed(name, status)</code> + narrowing" server="src/server/rpc/rpcLimited.ts" client="src/ui/demos/rpc/responses/TypedErrorDemo.abide" tab="server">
    <TypedErrorDemo />
  </Demo>
  <aside class="aside">
    <p>An untyped error tells you <em>that</em> it failed. <code>error.typed(name, status, schema?)</code> tells you <em>which</em> failure, by name — so a caller can narrow one kind from another with <code>fn.isError(e, name)</code> instead of matching on status codes or message strings.</p>
  </aside>
  <Demo id="redirect" title="<code>redirect(url, status)</code>" server="src/server/rpc/rpcGoHome.ts" client="src/ui/demos/rpc/responses/RedirectResponseDemo.abide" tab="server">
    <RedirectResponseDemo />
  </Demo>
  <aside class="aside">
    <p>The third kind of answer is "not here, there". <code>redirect(url, status=302)</code> throws a <code>Redirect</code> for the same reason <code>error()</code> throws an <code>HttpError</code> — a handler that redirects produced no value, so it must not resolve as one — and the router renders the 3xx. The browser fetch then follows it transparently — the resolved response reports <code>redirected</code> and the final <code>url</code>.</p>
  </aside>
  <Panel id="baseline-headers" title="The baseline every response carries">
    <ul class="grid">
      <li><code>X-Content-Type-Options: nosniff</code> and <code>Referrer-Policy: strict-origin-when-cross-origin</code> — on everything</li>
      <li><code>Cache-Control: private, no-cache</code> + <code>Vary: Cookie</code> — the identity-scoped default, since a response may depend on who asked. Content-addressed <code>/__abide/chunk/</code> assets opt out by declaring their own immutable long-cache</li>
      <li><code>Strict-Transport-Security</code> in production; <code>X-Frame-Options: SAMEORIGIN</code> on HTML documents; a <code>405</code> carries <code>Allow</code></li>
    </ul>
  </Panel>
  <aside class="aside">
    <p>Beyond each helper's own <code>Content-Type</code> / <code>Location</code>, the router stamps this baseline on every response at one choke point — and only where the response didn't already set it, so a handler, helper or middleware always keeps the last word.</p>
  </aside>
  <Demo id="raw" title="<code>fn.raw(args, init?)</code> — the untouched <code>Response</code>" server="src/server/rpc/rpcGreet.ts" client="src/ui/demos/rpc/responses/RawResponseDemo.abide" tab="client">
    <RawResponseDemo />
  </Demo>
  <aside class="aside">
    <p>Every card above shows the decoded result. <code>fn.raw(args, init?)</code> is the view underneath: the untouched <code>Response</code>, memo bypassed and nothing thrown, so you read the status line, headers and body yourself. Switch between the helpers and watch the status change — <code>200</code>, <code>422</code>, <code>429</code>, <code>302</code> — each chosen by the helper, none by the framework.</p>
    <p>The redirect button passes <code>{'{'} redirect: "manual" {'}'}</code> as <code>init</code>, the second argument of <code>raw</code>. The browser still won't show you the 302: the Fetch spec resolves a manual redirect to an opaque response with the status, <code>Location</code> and body withheld from JS. The wire carries all three — <code>curl -i</code> proves it — a reminder that <code>raw</code> hands you the browser's <code>Response</code>, not the server's.</p>
  </aside>
</main>
