<script module>
  import Demo from "$ui/components/Demo.abide"
  import KeyedListDemo from "$ui/demos/templating/lists/KeyedListDemo.abide"
  import KeylessListDemo from "$ui/demos/templating/lists/KeylessListDemo.abide"
</script>

<main class="page">
  <h1>Lists</h1>
  <p class="lead">A <code>&#123;#for&#125;</code> block renders one body per item. Key it by identity to move DOM nodes on add / remove / reorder, or leave it keyless to map by position.</p>
  <Demo id="keyed" title="Keyed <code>&#123;#for item, i of list by key&#125;</code>" client="src/ui/demos/templating/lists/KeyedListDemo.abide" tab="client">
    <KeyedListDemo />
  </Demo>
  <aside class="aside">
    <p>Keyed by <code>item.id</code>, the block reconciles by identity: adding, removing, and reordering move the matching DOM nodes instead of re-creating them.</p>
  </aside>
  <Demo id="keyless" title="Keyless positional <code>&#123;#for&#125;</code>" client="src/ui/demos/templating/lists/KeylessListDemo.abide" tab="client">
    <KeylessListDemo />
  </Demo>
  <aside class="aside">
    <p>Without a <code>by</code> clause the block maps by position — perfect for a plain value list.</p>
  </aside>
</main>
