<script>
  import { state } from "abide/shared/state"

  // bind:value two-way over a bare state var — the plain sugar, no hand-written accessor (#14).
  let value = state("hello")
</script>

<input id="value-input" bind:value={value} />
<p>Mirror: <span id="value-out">{value}</span></p>
