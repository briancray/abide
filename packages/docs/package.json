{
  "name": "docs",
  "private": true,
  "type": "module",
  "dependencies": {
    "@abide/bench": "workspace:*",
    "abide": "workspace:*",
    "bun-plugin-tailwind": "^0.1.2",
    "tailwindcss": "^4.3.3"
  },
  "scripts": {
    "dev": "abide dev",
    "build": "abide build",
    "start": "abide start",
    "e2e": "playwright test --project=chromium",
    "e2e:ci": "playwright test --project=chromium --retries=1",
    "e2e:perf": "playwright test --project=perf --workers=1",
    "e2e:all": "playwright test --project=chromium --retries=1 && playwright test --project=perf --workers=1",
    "typecheck": "tsc --noEmit",
    "abide-check": "abide check .",
    "abide-lsp": "bun scripts/lsp-dogfood.ts"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "@types/bun": "1.3.14",
    "typescript": "^7.0.0"
  }
}
