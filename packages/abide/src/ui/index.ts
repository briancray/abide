// The client runtime — what a compiled `.abide` bundle imports through `abide/runtime`.
// Template instantiation, keyed reconcile, event delegation and hydration. Every
// module reached from here is in the client's static closure, so an export added
// here is paid for on every page (CLAUDE.md, "seams and imports").

export {}
