// The server seam — what an app reaches through `abide/server`, plus the build.
// Request handling, SSR, the rpc boundary, and the `.abide` compiler under ./build.
// Nothing here may be reached from the client entry's static closure.

export {}
