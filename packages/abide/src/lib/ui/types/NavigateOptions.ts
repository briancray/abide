// Options for navigate(): replace the history entry instead of pushing, and
// whether to scroll to top after a successful push (ignored on replace).
export type NavigateOptions = { replace?: boolean; scroll?: boolean }
