/* An imported const the `OUTBOX_OPT` regex can't read: `outbox: OUTBOX_ENABLED` is not an
   inline `true`/`false` token, but the checker resolves its type to the literal `true`. */
export const OUTBOX_ENABLED = true
